**What is an inventory?**

Inventory is a file that tells Ansible which servers (managed nodes) to connect to and manage.

It can contain:

Hostnames or IP addresses


Server groups (e.g., web, database)


Connection details (user, SSH port, etc.)
